# Secrets — never commit real environment values.
.env
.env.*
!.env.example

# Build output (compiled binaries at repo root only — not the cmd/api source dir)
/out/
/bin/
/api
/api.exe
/dyd-api

# Go
*.test
*.out
coverage.*
vendor/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
